services:
  - type: web
    name: phoenix-restaurant
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: phoenix-db
          property: connectionString

databases:
  - name: phoenix-db
    plan: free
